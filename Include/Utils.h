#include <string>
#include <vector>

/* split string into substrings based on delimiter
*/
std::vector<std::string> tokenize(std::string str, std::string = " ");
/* set width and prepend 0s to a string
*/
std::string formatStringWidth(int num, int width);